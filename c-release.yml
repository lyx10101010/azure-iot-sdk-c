name: $(BuildID)_$(BuildDefinitionName)_$(SourceBranchName)_$(Date:yyyyMMdd)$(Rev:.r)

phases:

- phase: Setup
  displayName: My Phase
  condition: succeededOrFailed()
  queue:
    name: Windows 2019 with VS2019
